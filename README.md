# chat-server
Chat Server provides a platform for multiple users to interact with each other. The server acts as a middleware between two clients which helps in transporting information to and from the clients, thereby completing the peer-to-peer connection.
